defmacro etiquetas
	[a] if x != 0 goto b
		y <- y + x2
		goto e
	[b] y <- y + 2
		x--
		goto a
endmacro

defmacro dieciseis
z <- ocho()
z2 <- ocho()
y <- z + z2
endmacro

defmacro ocho
z <- cuatro()
z2 <- cuatro()
y <- z + z2
endmacro

#esto n o debería estar aquí

defmacro cuatro
z <- dos()
z2 <- dos()
y <- z + z2
endmacro

defmacro dos
Z <- uno()
z2 <- uno()
y <- z + z2
endmacro

defmacro uno
y <- 1
endmacro